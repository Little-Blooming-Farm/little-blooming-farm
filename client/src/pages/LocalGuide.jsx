import StoryPage from '../components/StoryPage.jsx';

export default function LocalGuide() {
  return (
    <StoryPage
      slug="local-guide"
      eyebrow="Within forty minutes"
      fallbackTitle="Local Guide"
      fallbackSubtitle="What we would do, if we were you, and we had a week."
      fallbackImage="/media/local/hero.jpg"
      tone="cream"
    />
  );
}
